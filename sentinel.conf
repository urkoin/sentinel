# specify path to allcoinguru.conf or leave blank
# default is the same as AllcoinguruCore
germanccd_conf=/home/ivan/.GermanccCore/germanccd.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
